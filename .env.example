# All three values are in your Neon dashboard → Auth tab
# (Neon labels them NEXT_PUBLIC_* for Next.js — the values are the same for Vite)
VITE_NEON_AUTH_URL=
VITE_STACK_PROJECT_ID=
VITE_STACK_PUBLISHABLE_KEY=
